#include "main.h"

/**
 * main - tests function that prints if integer is positive or negative
 *
 * Description: tests the return value of the function on case 0
 * Return: 0
 */
int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);
	return (0);
}
